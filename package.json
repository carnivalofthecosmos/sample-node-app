{
  "name": "sample",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "$(aws ecr get-login --no-include-email --region ap-southeast-2) && docker build -t ${ECR_URL}:latest . && docker push ${ECR_URL}:latest"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
